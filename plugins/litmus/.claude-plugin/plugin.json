{
  "name": "litmus",
  "description": "Litmus — live-verify a defect/fix end-to-end before marking it done: reproduce the ticket's steps in the real UI as the specified user, instrument the running app (fetch+XHR hook / front-end scope / DB relay), prove the defect AND the fix, then update the tracker. Backend curl / unit / DB-only / 'deployed' do NOT count.",
  "version": "1.0.0",
  "author": {
    "name": "hacka0wi"
  },
  "keywords": ["verify", "live-test", "reproduce", "litmus", "qa", "chrome-mcp", "defect"]
}
